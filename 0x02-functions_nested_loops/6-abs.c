#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: parameter to test
 *
 * Return: 1 or 0 or -1 (Successfull)
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);

	return (n);
}
